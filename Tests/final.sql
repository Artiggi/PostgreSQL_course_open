-- ============================================================================
-- КОНТРОЛЬНАЯ РАБОТА 2
-- (адаптирована для учебной БД PostgresPro: https://postgrespro.ru/education/demodb)
-- ============================================================================

-- Требования к решению:
-- - все скрипты должны выполняться без ошибок;
-- - имена колонок должны быть в точности такими, как указано в задании;
-- - если в задании указана сортировка, её нужно соблюсти.


-- ============================================================================
-- ЗАДАЧА 1. Агрегация + JOIN + HAVING 
-- ============================================================================
-- Для каждого аэропорта вылета (departure_airport) посчитайте:
-- 1) segments_count      - количество сегментов,
-- 2) avg_segment_price   - средняя цена сегмента,
-- 3) max_segment_price   - максимальная цена сегмента.
--
-- Используйте JOIN таблиц routes + flights + segments.
--
-- В результат включите только аэропорты, где:
-- - segments_count >= 100
-- - avg_segment_price >= 2000
--
-- Столбцы вывода:
-- departure_airport, segments_count, avg_segment_price, max_segment_price.
--
-- avg_segment_price и max_segment_price округлите до 2 знаков.
-- Сортировка по avg_segment_price segments_count по убыванию.
-- ============================================================================

-- Решение:



-- ============================================================================
-- ЗАДАЧА 2. CTE + подзапрос + анти-join логика
-- ============================================================================
-- Найдите маршруты (route_no), которые:
-- 1) имели минимум 15 рейсов,
-- 2) никогда не имели статус 'Cancelled',
-- 3) имеют среднюю цену сегмента выше глобальной средней цены сегмента
--    по всей таблице segments.
--
-- Сделайте решение через CTE
--
-- Столбцы вывода:
-- route_no, flights_count, cancelled_count, route_avg_price, global_avg_price, price_diff.
--
-- route_avg_price, global_avg_price, price_diff округлите до 2 знаков.
-- Сортировка по price_diff flights_count по убыванию.
-- ============================================================================

-- Решение:



-- ============================================================================
-- ЗАДАЧА 3. Оконные функции
-- ============================================================================
-- Для таблицы segments выведите сегменты и их позицию по цене внутри класса.
--
-- Для каждого fare_conditions посчитайте:
-- 1) row_number по убыванию price,
-- 2) среднюю цену внутри класса (оконной функцией),
-- 3) отклонение цены сегмента от средней цены класса.
--
-- Столбцы вывода:
-- ticket_no, flight_id, fare_conditions, price, price_rank, class_avg_price, diff_from_class_avg.
-- class_avg_price и diff_from_class_avg округлить до 2 знаков.
--
-- В итог включите только топ-3 самых дорогих сегмента в каждом классе.
-- Отсортируйте по fare_conditions, затем по price_rank.
-- ============================================================================

-- Решение:



-- ============================================================================
-- ЗАДАЧА 4. Строковые функции + JSON + агрегация 
-- ============================================================================
-- Используя airports_data:
--
-- 4.1 Выведите по каждому городу (ru):
-- - city_ru,
-- - airports_count,
-- - airport_codes_csv              (STRING_AGG с разделителем  ",") кодов аэропортов ),
-- - airport_names_ru_csv           (STRING_AGG с разделителем  ",") русских названий аэропортов),
-- - city_label                     (UPPER(city_ru) || ' / ' || airports_count).
--
-- 4.2 Включите только города, где более 1 аэропорта.
--
-- Столбцы вывода:
-- city_ru, airports_count, airport_codes_csv, airport_names_ru_csv, city_label.
--
-- Сортировка: airports_count DESC, city_ru.
-- ============================================================================

-- Решение:



-- ============================================================================
-- ЗАДАЧА 5. TEMP TABLE + DDL/DML + аналитика
-- ============================================================================
-- Подготовьте временную витрину и посчитайте метрики.
--
-- 5.1 Создайте TEMP-таблицу temp_flight_revenue с колонками:
-- flight_id, route_no, status, departure_airport, scheduled_departure,
-- revenue, tickets_count.
--
-- 5.2 Заполните её из flights + routes + segments:
-- - revenue      = сумма price по flight_id,
-- - tickets_count = число DISTINCT ticket_no по flight_id.
--
-- 5.3 Добавьте колонку revenue_bucket text и обновите её значение на:
-- - 'HIGH'   если revenue >= 1000000
-- - 'MEDIUM' если revenue >= 300000 и < 1000000
-- - 'LOW'    иначе
--
-- 5.4 Постройте запрос-отчёт по departure_airport и revenue_bucket:
-- - flights_count,
-- - avg_revenue,
-- - max_revenue.
--
-- Столбцы вывода отчёта:
-- departure_airport, revenue_bucket, flights_count, avg_revenue, max_revenue.
-- avg_revenue и max_revenue округлить до 2 знаков.
-- Сортировка: departure_airport, revenue_bucket.
-- ============================================================================

-- Решение 5.1:

-- Решение 5.2:


-- Решение 5.3:


-- Решение 5.4:
